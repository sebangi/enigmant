{% if themeCourant is defined %}
    <a href="{{ path("grade.index", {themeCourant: themeCourant}) }}" class="btn btn-outline-success ma-nav">
{% else %}
    <a href="{{ path("grade.index", {themeCourant: "Chêne"}) }}" class="btn btn-outline-success ma-nav">
{% endif %}

    {% if themeCourant is defined %}
        {{ app.user.getGrade( themeCourant ) ~ " " }}    
    {% endif %}
        <span class="font-weight-bold">
           {{ app.user.getUsername() }}
        </span>
    <span class="fas fa-medal" aria-hidden="true"></span>
</a>
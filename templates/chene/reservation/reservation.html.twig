{% extends "chene/baseChene.html.twig" %}

{% block body %}
    <div class="jumbotron mon-jumbotron">
        <div class="container">
            <h1 class="text-center">Location du Jeu en Chêne {{ jeu.nom }}</h1>
        </div>
    </div>

    {{ include('outils/_flash.html.twig') }}  

    {{ include('chene/reservation/_progressBar.html.twig', { etape : "1" }) }}

    <div class="container mt-4">
        <h5 class="font-weight-bold text-center mb-3">
            Avant de procéder à la location, veuillez confirmer les conditions suivantes : 
        </h5>

        <div class="card mb-4 border-dark">
            <div class="card-header bg-dark text-white">
                <h5 class="mb-0 mt-0">Lieu de retrait</h5>
            </div>
            <div class="card-body  p-3">
                <div class="container">
                    <div class="row">
                        <h6 class="col-md-9 text-justify mb-0 border-right border-dark">
                            <ul class="mb-0">
                                <li class="mb-2">
                                    Le Jeu en Chêne est à retirer à <b>Saint Philbert de Grand Lieu</b> (Loire-Atlantique).
                                </li>
                                <li>
                                    Il est également possible de retirer le Jeu sur rendez-vous <b>dans la région Nantes sud-Loire</b>.
                                </li>
                            </ul>
                        </h6>

                        <div class="col-md-3  justify-content-center align-self-center">    
                            <div class="custom-control-lg custom-control custom-checkbox">
                                <input class="custom-control-input" id="checkbox-large1" type="checkbox" name='checkboxValidation'/>
                                <label class="custom-control-label" for="checkbox-large1">
                                    <h5>Cela me convient</h5>
                                </label>
                            </div>
                        </div> 

                    </div>                    
                </div>
            </div>
        </div>
        
    </div>

    <div class="text-center">
          <button id="enable-on-two" disabled name='next2' 
                  onclick="location.href='{{ path('chene.location', { slug: jeu.slug, id: jeu.id, etape:"2" }) }}'" 
                  class="text-center btn btn-success mt-2" type="button">
              Louer le Jeu en Chêne {{ jeu.nom }}
          </button>
          
          
      
    </div> 

</div>


{% endblock %}
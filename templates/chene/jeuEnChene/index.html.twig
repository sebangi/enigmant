{% extends "chene/baseChene.html.twig" %}

{% block body %}
    <div class="jumbotron mon-jumbotron  text-center">
            <div class="container">
            <h1>Il existe actuellement {{ nb_jeux_en_chene }} jeux en chêne</h1>
            <hr>
            {{ form_start(form) }}
            <div class="form-row align-items-end">
                <div class="col form-group no-margin">
                    {{ form_widget(form.maxPrix) }}
                </div>
                <div class="col form-group no-margin">
                    {{ form_widget(form.minDifficulteRaisonnement) }}
                </div>
                <div class="col">
                    <div class="form-group no-margin">
                        <button class="btn btn-primary">Rechercher</button>
                    </div>
                </div>
            </div>
            {{ form_end(form) }}
        </div>
    </div>
    <div class="container mt-4 ">
            
        <div class="row mt-4">
            {% for jeuEnChene in jeux_en_chene  %}
                <div class="d-flex align-items-stretch col-md-4">
                    {%  include 'chene/jeuEnChene/_jeuEnChene.html.twig' %}
                </div>
            {% endfor %}
        </div>

        {# display navigation #}
        <div class="pagination">
            {{ knp_pagination_render(jeux_en_chene) }}
        </div>
    </div>
{% endblock body %}

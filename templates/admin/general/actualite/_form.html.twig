{{ form_start(form) }}
<div class="row">
    <div class="col-md-6">{{ form_row(form.titre) }}</div>
    <div class="col-md-2">{{ form_row(form.theme) }}</div>
    <div class="col-md-2">{{ form_row(form.imageFile) }}</div>
    <div class="col-md-2">{{ form_row(form.prioritaire) }}</div>
</div>
<div class="row">
    <div class="col-md-12">{{ form_row(form.date) }}</div>
</div>

<div class="row">
    <div class="col-md-12">{{ form_row(form.texte) }}</div>
</div>

<div class="row">
    <div class="col-md-4">{{ form_row(form.collectionChene) }}</div>
    <div class="col-md-4">{{ form_row(form.jeuEnChene) }}</div>
    <div class="col-md-4">{{ form_row(form.babiole) }}</div>
</div>

{{ form_widget(form) }}

<button class="btn btn-primary">{{ button|default('Enregistrer') }}</button>
{{ form_end(form) }} 
